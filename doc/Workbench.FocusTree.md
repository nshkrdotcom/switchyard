# `Workbench.FocusTree`

Derived focus traversal metadata.

# `t`

```elixir
@type t() :: %Workbench.FocusTree{paths: [[term()]]}
```

# `build`

```elixir
@spec build(Workbench.RenderTree.t()) :: t()
```

---

*Consult [api-reference.md](api-reference.md) for complete listing*
