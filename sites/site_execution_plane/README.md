# Switchyard Site Execution Plane

`site_execution_plane` is the raw substrate/admin surface for Switchyard.

It renders brokered `execution_plane` process sessions, operator terminals, and
job state inside the Switchyard product shell without claiming runtime
ownership.
